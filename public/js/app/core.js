var core, testData;

core = new Backbone.Marionette.Application();

core.addRegions({
  AppContainer: ".app-container",
  ListOfProjects: ".app-projects-list",
  ListedHeader: ".app-projects-header",
  ListedFooter: ".app-projects-footer"
});

core.addInitializer(function() {
  var ProjectList, mainView;
  ProjectList = App.View.ProjectList;
  mainView = new ProjectList;
  return core.AppContainer.show(mainView);
});

testData = function(numberOfModels) {
  var m, modelList, x, _i, _ref;
  console.warn("Model Creation");
  modelList = [];
  for (x = _i = 0, _ref = numberOfModels - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; x = 0 <= _ref ? ++_i : --_i) {
    m = new App.Model({
      name: "Model " + x,
      description: "just wow " + x
    });
    modelList.push(m);
  }
  return App.projects.add(modelList);
};

App.core = core;

$(document).ready(function() {
  console.log("Starting App");
  App.core.start();
  console.log("Adding test models");
  testData(4);
  console.log(window.App);
  console.log(App.projects);
  console.log("%cwriting..", "font-weight:900");
  /*
  update = App.projects.sync "create", App.projects,
    success: (a) -> 
      console.log "success!" 
    error: (a) -> 
      console.log "not a success.."
  */

  console.log("%creading..", "font-weight:900");
  /*
  read = App.projects.sync "read", App.projects,
    success: (a) -> 
      console.log "success!" 
    error: (a) -> 
      console.log "not a success.."
  */

  return console.log(App.projects);
});

// Generated by CoffeeScript 1.5.0-pre
